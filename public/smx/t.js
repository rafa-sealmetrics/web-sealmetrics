// Fichero de prueba temporal — test de bloqueo de Brave (Shields agresivo).
// Comprueba si un path NEUTRO, sin marca, sobrevive en un dominio real con HTTPS.
// Borrar junto con /public/brave-test/ y /public/wp-json/ cuando acabe la prueba.
window.__smTest = window.__smTest || {};
window.__smTest.neutral = true;
